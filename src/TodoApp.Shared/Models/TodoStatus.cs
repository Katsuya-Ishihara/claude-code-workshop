namespace TodoApp.Shared.Models;

public enum TodoStatus
{
    NotStarted = 0,
    InProgress = 1,
    Completed = 2
}

public enum Priority
{
    Low = 0,
    Medium = 1,
    High = 2
}

public enum UserRole
{
    Member = 0,
    Admin = 1
}
